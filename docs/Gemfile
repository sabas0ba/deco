# Only for previewing the documentation site locally. GitHub Pages builds these
# pages itself from `main`, with this same gem, so nothing here is a dependency
# of deco — the editor builds with cargo alone.
#
#     cd docs && bundle install && bundle exec jekyll serve

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
