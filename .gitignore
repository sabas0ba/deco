/target
**/*.rs.bk
Cargo.lock.orig

# Node extension host
node_modules/
extension-host/dist/
extension-host/*.tsbuildinfo

# Local editor state / test scratch
.deco/
*.log

# Release staging produced by `cargo xtask dist`
/dist

# Local `jekyll serve` of the documentation site; GitHub Pages builds its own
docs/_site/
docs/.jekyll-cache/
docs/.jekyll-metadata
docs/Gemfile.lock
